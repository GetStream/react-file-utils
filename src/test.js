import {
  AttachmentIcon,
  FilePreviewer,
  FileUploadButton,
  ImageDropzone,
  ImagePreviewer,
  PictureIcon,
  ImageUploadButton,
  FileIcon,
  Thumbnail,
  ThumbnailPlaceholder,
  LoadingIndicator,
  IconButton,
} from './';

describe('AttachmentIcon', () => {
  it('is truthy', () => {
    expect(AttachmentIcon).toBeTruthy();
  });
});

describe('FilePreviewer', () => {
  it('is truthy', () => {
    expect(FilePreviewer).toBeTruthy();
  });
});

describe('FileUploadButton', () => {
  it('is truthy', () => {
    expect(FileUploadButton).toBeTruthy();
  });
});

describe('ImageDropzone', () => {
  it('is truthy', () => {
    expect(ImageDropzone).toBeTruthy();
  });
});

describe('ImagePreviewer', () => {
  it('is truthy', () => {
    expect(ImagePreviewer).toBeTruthy();
  });
});

describe('PictureIcon', () => {
  it('is truthy', () => {
    expect(PictureIcon).toBeTruthy();
  });
});

describe('ImageUploadButton', () => {
  it('is truthy', () => {
    expect(ImageUploadButton).toBeTruthy();
  });
});

describe('FileIcon', () => {
  it('is truthy', () => {
    expect(FileIcon).toBeTruthy();
  });
});

describe('Thumbnail', () => {
  it('is truthy', () => {
    expect(Thumbnail).toBeTruthy();
  });
});

describe('ThumbnailPlaceholder', () => {
  it('is truthy', () => {
    expect(ThumbnailPlaceholder).toBeTruthy();
  });
});

describe('LoadingIndicator', () => {
  it('is truthy', () => {
    expect(LoadingIndicator).toBeTruthy();
  });
});

describe('IconButton', () => {
  it('is truthy', () => {
    expect(IconButton).toBeTruthy();
  });
});
